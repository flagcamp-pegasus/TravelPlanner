/**
 * 
 * @author
 *
 */

package db.mysql;

import java.sql.DriverManager;
import java.sql.Statement;
import java.sql.Connection;

public class MySQLTableCreation {
	/**
	 * 
	 * TABLE1: user's info: (user_name, password, first_name, last_name)
	 * TABLE2: city's info: (city_name, city_id, image_url)
	 * TABLE3: route's info: (user_id, ith_day, route)
	 * up to you to decide how to save the "route" in DB
	 */
	public static void main(String[] args) {
		
	}
	
}

